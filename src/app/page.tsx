import { Button } from "@/components/ui/button";

interface HomeProps {}

const Home: React.FC<HomeProps> = () => {
  return (
    <div>
      <Button>HomePage</Button>
    </div>
  );
};

export default Home;
